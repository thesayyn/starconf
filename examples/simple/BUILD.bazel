load("@starconf//:autoconf.bzl", "autoconf")
load("@rules_cc//cc:defs.bzl", "cc_library")

autoconf(
    name = "config",
    config_in = "config.h.in",
    starlark_config = "autoconf.star"
)

cc_library(
    name = "lib",
    srcs = [
        "main.c"
    ],
    deps = [":config"]
)
